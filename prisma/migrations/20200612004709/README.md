# Migration `20200612004709`

This migration has been generated by Niputi at 6/12/2020, 12:47:09 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `test`.`User` (
`activated` int NOT NULL DEFAULT 0 ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`isAdmin` boolean NOT NULL DEFAULT false ,`name` varchar(191)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `test`.`HaircutOrder` (
`authorId` int  ,`content` varchar(191)   ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`published` boolean NOT NULL DEFAULT false ,`title` varchar(191) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `test`.`Sessions` (
`data` json NOT NULL  ,`expires` datetime(3) NOT NULL  ,`session_id` varchar(191) NOT NULL  ,
    PRIMARY KEY (`session_id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `test`.`User`(`email`)

ALTER TABLE `test`.`HaircutOrder` ADD FOREIGN KEY (`authorId`) REFERENCES `test`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

DROP TABLE `test`.`_migration`;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200612004709
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource mysql {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+model User {
+  id    Int     @default(autoincrement()) @id
+  name  String?
+  createdAt DateTime @default(now())
+  HaircutOrder HaircutOrder[]
+  email String  @unique
+  isAdmin Boolean @default(false)
+  activated Int @default(0)
+}
+
+model HaircutOrder {
+  id        Int     @default(autoincrement()) @id
+  createdAt  DateTime   @default(now())
+  authorId  Int?
+  content   String?
+  published Boolean @default(false)
+  title     String
+  author    User?   @relation(fields: [authorId], references: [id])
+}
+
+model Sessions {
+  session_id String @id
+  expires DateTime
+  data Json
+}
```


